---
- name: Install gnupg2 so git ppa can be added
  ansible.builtin.apt:
    name:
      - gnupg2
  become: true

- name: Install vim via ansible.builtin.apt
  ansible.builtin.apt:
    name: "{{ packages }}"
  vars:
    packages:
      - vim=*
      - vim-gui-common=*
  become: true
